// Basic JavaScript for portfolio

// Display a message in the browser console when the page loads.
window.onload = function () {
    console.log("Abhishek Portfolio Loaded Successfully");
};

// Show a simple message when a contact link is selected.
// The WhatsApp and email links still work through their own URLs.
function showContactMessage() {
    alert("Please use the WhatsApp or Email button to contact me.");
}
